# Playpen
This directory is intended to contain snippets and experiments that were formed while exploring an idea

## Prereqs
*  `npm install -g tsx`
    * Then you run the scripts as `tsx hello_world.ts`
